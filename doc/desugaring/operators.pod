=head2 The Ternary Operator

	condition ? statement1 : statement2

Equivalent:

	if( condition ) {
		statement1;
	} else {
		statement2;
	}

=head2 Arithmetic Operators

	a += 5
	a++
	a--
	b *= 7
	b /= 10

Equivalent:

	a = a + 5
	a = a + 1
	a = a - 1
	b = b * 7
	b = b / 10

